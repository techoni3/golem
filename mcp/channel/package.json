{
  "name": "@golem/channel-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Custom MCP channel server for the golem substrate — pushes briefs, interrupts, halts, and gate verdicts into a live golem-ceo Claude Code session.",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "check": "node scripts/smoke.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
