# Real-process journey testing

`packages/testkit` owns the small, production-shaped test boundary for the selected J1–J8 journeys. It creates an isolated temporary `HOME`/`GOLEM_HOME`, allowlists only locale, timezone, temp, and executable-path environment values, starts detached child groups, and sends `SIGTERM` before a bounded `SIGKILL` fallback. Every created artifact must be contained by the supplied temporary root; cleanup removes that root.

`test/journeys/run.mjs` is the serial root runner. Its scenario registry names the journey, tier, and catastrophic regression protected by each check. It prints the versioned `golem-journey-summary/v1` projection in stable key and scenario order. Runtime port, PID, timestamp, path, UUID, and secret values are diagnostic-only: semantic comparison delegates to the GOL-24 legacy normalizer, which keeps readiness, lifecycle ordering, and identifier relationships meaningful.

The selected checks deliberately cross process, SQLite, HTTP, WebSocket, native executable, and headless Playwright boundaries. `control-plane-auth-ws-lifecycle` is the GOL-32 J6 Fastify boundary: isolated compiled foreground/invalid-response children prove generated-client bearer mutation without Origin; bounded cookie+CSRF failure modes; GOL-26 request/output envelopes; valid, compacted, and restarted WS cursors; legacy health/meta/static compatibility; nonce-safe lock recovery; injected atomic LaunchAgent verbs; cleanup; and a fresh headless browser shell. The legacy-baseline scenario is an invocation adapter over frozen GOL-24, not a second implementation. The browser helper launches headless with a fresh context and writes trace/screenshot artifacts only after failure.

`materializer-crash-matrix` is the GOL-34 J3 durable-runtime proof. It executes 100 independent native filesystem producers plus duplicate event ids against real SQLite; proves active claims are not reclaimed before expiry, bounded poison quarantine, no-clobber archive bytes, child materializer crash positions, restart dedupe, stale watermark evidence, redacted outbox retries/ack-CAS accounting, and exact pending/retried/published/permanent states through an independent SQLite idempotent consumer. `dashboard-down-inbox-replay` is a distinct J1 process boundary: hook-compatible producers write the inbox while no service/runtime SQLite exists, then a fresh control-plane/scheduler start replays and archives all envelopes. These are the only GOL-34 scenarios; their child producer fixtures are process tools, not a test farm.
`materializer-crash-matrix` is the GOL-34 J3 durable-runtime proof. It executes 100 independent native filesystem producers plus duplicate event ids against real SQLite; proves active claims are not reclaimed before expiry, bounded poison quarantine, no-clobber archive bytes, child materializer crash positions, restart dedupe, stale watermark evidence, redacted outbox retries/ack-CAS accounting, and exact pending/retried/published/permanent states through an independent SQLite idempotent consumer. `dashboard-down-inbox-replay` is a distinct J1 process boundary: hook-compatible producers write the inbox while no service/runtime SQLite exists, then a fresh control-plane/scheduler start replays and archives all envelopes.

GOL-36 keeps tracker extraction proof in two distinct J4 SQLite/process selectors without a fixture farm. `delivery-queue-crash-matrix` covers claim authority, semantic idempotency, retry/deadline/dead-letter, crash/reclaim, and redacted audit. `bus-offline-replay` covers immutable event dedupe, offline-to-active ordered cursor replay, passive lease updates, and dependency-aware retention. Neither needs a listener or credentials.

`UNMET` is an explicit environment gate, not a pass. In particular, a sandbox that rejects `127.0.0.1` listeners with `EPERM` is reported separately from a product regression. `PASS` means the boundary actually completed; `FAIL` preserves a redacted diagnostic and a non-zero exit. The runner is serial by design so it owns no shared port, profile, user home, or process cleanup state.

GOL-46 keeps its projection evidence compact: `test/runtime/runtime-projections.test.mjs`
uses real runtime SQLite rows and two foreground Fastify instances to prove the
live/history/diagnostic HTTP contract and authenticated WebSocket snapshot,
delta, cursor, and restart-resync behavior. The two journey selectors are
`live-history-diagnostics` and `projection-ws-restart-resync`; no projection
unit or mock fan-out is allowed.
