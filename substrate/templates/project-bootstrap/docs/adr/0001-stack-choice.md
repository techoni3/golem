# 0001: Stack choice

## Status

Proposed
<!-- Tech Architect promotes to Accepted after the stack is locked in. -->

## Context

{{PROJECT_NAME}} bootstrapped on {{DATE}}. The project's product specs are
the input; this ADR records the stack chosen to deliver them.

<!-- Tech Architect: replace with the real context — what the product asks
     for, what the team is comfortable with, what constraints matter
     (latency, cost, team size, deploy target). -->

## Decision

<!-- Tech Architect: state the chosen stack in imperative voice.

     Example:
     We will use FastAPI (Python 3.12) for the API layer, SQLAlchemy 2.x +
     Alembic on Postgres 16 for persistence, Pytest for tests, and ruff +
     mypy strict for lint/type. Local dev via uv; deploy via Docker.
-->

## Consequences

- Positive: …
- Negative / trade-off: …

## Alternatives considered

<!-- One bullet per stack we considered and rejected, with one-sentence reason. -->
