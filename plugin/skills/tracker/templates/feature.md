# Work item: <one-line result>

The body is this item's implementation plan. Intent lives in the parent spec — point, do not
restate; the builder reads the chain.

## Scope

Exactly what this item changes, and its boundary with sibling items. One writer per checkout: no
sibling in the same wave touches the same files.

## Plan

The concrete approach: files and touch points, the order of changes, and anything the grounding
already learned that the builder should not rediscover. Notes here are accelerators, not
boundaries — builders verify from source and comment when a note is stale or wrong.

## Acceptance

Observable outcomes, derived from the parent's acceptance — not tasks.

- [ ] <outcome>

## Not in this item

Adjacent work that is deliberately deferred or belongs to a sibling. Discovered scope goes on the
ticket as a comment, not into the diff.
