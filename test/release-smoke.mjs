// Compatibility entry point for callers that still invoke the former smoke
// script directly. The packed-package verifier supersedes its legacy nested-MCP
// assumptions and exercises the complete release boundary.
import "./package-verification.mjs";
